# gpt-2
Implementation of GPT-2 in JAX


To get started:

```bash
python3 -m venv .venv
. .venv/bin/activate
pip install --upgrade pip
pip install -U 'jax[cuda13]' jupyterlab IPython matplotlib numpy
```
